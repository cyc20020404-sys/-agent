
**技术栈**：Vue 3 + Spring Boot + MySQL + Redis + RabbitMQ + InfluxDB + Docker

**空气技研管理平台（CAMS）**

面向洁净室的空调风机设备管理系统，主要负责空调指令控制、传感器数据监测两个核心模块。对接下游硬件通信规范，通过 MQTT/Modbus实现软件到 PLC 硬件的完整控制，基于传感器时序数据实现运维查询与智能告警。

- **多线程并发控制 + 快照回滚**：CompletableFuture + 自定义线程池实现多设备并发，响应 O(n)→O(max)；写前全量快照，中途失败逆序回滚已写点位；REQUIRES_NEW 事务隔离保证单台失败不影响其余
- **RabbitMQ 异步解耦**：请求秒返 taskId，Consumer 异步执行；Direct Exchange 按 AHU/VAV 独立路由；死信队列处理超时异常 + 钉钉告警；Redis SETNX 幂等防重
- **MQTT 通信与消息可靠性**：对接下游硬件设备 MQTT 协议，按 Topic 设计路由接收上报数据和下发指令；QoS + retained + 断线自动重连，保障通道稳定可靠。
- **传感器数据监测**：面向洁净室的空气质量实时监控与智能告警，采集温湿度、CO₂、洁净空气量（CADR）、滤网压力等指标；传感器数据定时同步至 InfluxDB 时序库，Continuous Query 自动降采样 + Retention Policy 分层存储
- **指令下发与 ACK 闭环**：指令先写库再发 MQTT，构建 ACK 状态机，定时 + 主动轮询双路判定执行结果，实现下发—执行—确认全链路闭环，消息不丢、可追溯。



**数据对标功能开发**
**技术栈**：Spring Boot + MyBatis-Plus + MySQL + Redis + APIFOX

**项目简介**：负责「车辆材料对标管理」模块的设计与实现。旨在通过采集、整合多品牌车型的全方位配置数据，构建多维度的横向对比系统，直观呈现不同品牌车辆的材料与配置差异。

- **动态表结构设计**：采用「配置驱动 + 参数值明细表」设计动态管理对标参数，仅需维护配置数据，无需改表改码，支撑千级参数的高效录入与多车型横向对比。
- **对比查询性能优化**：针对千级参数、多车型的横向对比，按用户所选参数动态构造查询（联合索引控制扫描范围，避免全量加载），将参数明细行在 Service 层组装为横向对比结构；热门车型对比结果缓存至 Redis，命中直接返回，保证对比页秒开。
- **对标数据导出**：支持对标结果与对比报告导出，基于 EasyExcel 流式写出，配合分批查询，稳定处理大批量数据、避免内存溢出。
- **缓存一致性与防击穿优化**：对标模板、参数配置等基础数据及热门车型的对比结果被高频读取，引入 Redis 缓存。采用 Cache-Aside 策略，对标配置更新时先更新数据库、再删除缓存，配合延迟双删保证主从架构下的最终一致性；针对热门对标模板等热点 Key，用互斥锁重建缓存兜底，防止缓存击穿。



**情感陪伴机器人（核心研发）**

**项目介绍**：作为核心研发成员，负责机器人「情感大脑」的构建，通过大模型微调、RAG 检索增强，实现具备高共情能力与自主交互特性的本地化部署模型。

- **模型训练与蒸馏**：基于专有人设数据集，利用 SFT（监督微调）和 DPO（直接偏好优化）技术对基座模型进行微调，塑造特定的人格特质与对话风格。应用模型蒸馏（Distillation）技术，将超大规模模型的通用认知与逻辑能力迁移至轻量化本地模型，在保证推理速度的同时维持高性能交互。
- **长效记忆与 RAG 系统设计**：设计并实现基于 RAG 的动态知识库，实时记录并向量化存储用户对话细节。通过构建用户画像与情感记忆链，优化检索精度，使模型能基于历史上下文提供深度共情的个性化回复。
- **主动交互 Agent 设计**：编写复杂 Prompt 链并基于 LLM Agent 框架设计主动对话逻辑，使机器人能够根据时间、节日或用户情绪状态，在特定情境下自主发起对话，突破被动问答模式。

---

## 项目经历

### 智慧课堂 ｜ Java 独立开发 ｜ 2025.07-2025.12

**技术栈**：Spring Cloud Alibaba（Nacos + Sentinel + Gateway）、MyBatis-Plus、Redis、RabbitMQ、Elasticsearch + Canal

**项目简介**：基于 Spring Cloud Alibaba 微服务架构的在线教育平台，面向成人职业技能培训，拆分为 9 个微服务：网关、认证、用户、课程、学习、考试、促销、交易、媒体。

- **网关鉴权**：设计全局鉴权过滤器 `AuthGlobalFilter` 统一做 JWT 校验和用户信息透传，采用 AccessToken + RefreshToken 双 Token 机制实现无感刷新，并用 Redis 黑名单解决 JWT 无状态、无法主动失效的问题，支持踢人下线。
- **数据同步**：课程搜索用 Canal 监听 MySQL Binlog（伪装成 Slave 实时解析增删改），经 RabbitMQ 异步投递到 Elasticsearch，配合消费重试 + 死信队列 + XXL-JOB 定时对账保证最终一致性，避免「双写」的数据不一致。
- **学习中心**：签到用 Redis Bitmap 实现（1 人 1 月仅 31 位，1 万用户月存储从 15MB 降到 40KB）；学习进度用 Redisson 分布式锁（锁粒度到「用户 + 课时」）防并发重复累加，再经 MQ 削峰异步落库，记录 lastLearnTime 支持断点续播。
- **秒杀系统**：核心库存扣减用 Redis Lua 原子脚本防超卖，MD5 动态链接防脚本提前下单，SETNX 防一人重复抢购，扣减成功后 MQ 异步下单削峰，整体 QPS 从 DB 方案的 3000 提升到 10 万+。
- **分布式事务**：下单链路跨「订单 + 优惠券 + 积分」三个服务，普通下单用 Seata AT（无侵入、自动反向 SQL），秒杀高并发用 TCC（Try/Confirm/Cancel 高性能），积分等非实时链路用 MQ 最终一致性。

### 外卖客服 Agent 助手 ｜ 独立开发 ｜ 2025.11-2026.02

**技术栈**：Python + LangGraph + FastAPI + Spring Boot + Vue 3 + RAG

**项目介绍**：基于外卖业务构建的端到端 AI 客服系统，覆盖完整链路，实现让 LLM 真正操作业务系统（查单、催单、取消等）。

- **多 Agent 协作架构**：基于 LangGraph StateGraph 实现 Supervisor 编排模式，将任务分解给 4 个专业化 Agent，LLM 动态分析意图并条件路由，最终拼装回复。
- **自研 MCP 工具协议层**：参照 Anthropic MCP 规范，用装饰器模式实现工具注册/发现/调用框架，映射 9 个 MCP Tool → 15+ 个 Spring Boot 业务 API，让 LLM 从「回答问题」升级为「执行真实业务操作」。
- **双角色权限隔离**：通过 auth_type 区分 admin/user，消费者仅可调用 user_* 工具集查询自己的订单；下游 Spring Boot 以管理端、消费端两套独立 JWT 拦截器兜底，AI 层分流错误也不会数据互串。
- **知识库 RAG 问答**：Query Rewrite 将口语化查询改写为检索词 → FAISS 向量检索 → LLM 生成回答，覆盖菜单/菜品推荐、退款政策、下单流程等 FAQ。
- **三层记忆架构**：Working Memory（进程内存）/ Short-Term（Redis，30min）/ Long-Term（FAISS 磁盘持久化）。
- **合规审查双阶段流水线**：规则引擎（正则+关键词，毫秒级）先快筛，仅命中风险才触发 LLM 深度审查，90% 正常回复走快路径，兼顾安全与 token 成本。

---

## 面试拷打进度记录（2026-08-19）

### ✅ 已通关（能对着代码讲清楚）

- **① 指令链路**：三模式动作矩阵（autoOff 全关 / autoOn 按 minOa、RA=OA×0.7、新风 50% / filter 逐字段+新风固定 50%）；三种换算类型；开关线圈(FC=05) vs 开度寄存器(FC=06)
- **② 并发回滚**：REQUIRES_NEW + AOP 代理失效（拆独立 Bean）；batchWrite 四步（预加载→快照→写入→逆序回滚）；rollbackFail = 回滚失败需人工介入
- **③ 实时推送**：Redis Pub/Sub → StateChangeListener → SimpleBroker 两跳；推拉结合（推事件不推值）；Redis vs SimpleBroker 职责区分
- **④ RabbitMQ 异步**：链路全貌、SETNX 幂等（成功留 key / 失败删 key）、死信 3 触发 + DLX 路由、补偿（先写表后发 MQ）、Direct/Fanout/Topic、手动 ACK + 崩溃场景、Connection vs Channel、协议级四道保证、Kafka 对比
- **⑤ 传感器数据监测 + InfluxDB**（两轮，2026-08-26）：Line Protocol = `sensor_data,sensor_id=SN-001 temperature=23.5,... <ms时间戳>`（measurement=sensor_data / tag=sensor_id / 37 个 field / 毫秒时间戳）；sensor_id 用 tag 不用 field（tag 有索引、可 WHERE/GROUP BY，field 无索引只做数值聚合）；三层 RP（rp_raw 7d / rp_1h 90d / rp_1d 365d）；CQ 逐级降采样（raw→1h→1d 逐级 mean，每层在已粗化数据上再聚合，省算力省空间）；列式存储（同类型列好压缩 + 单列聚合只读该列）；tag 基数陷阱（高基数连续值如温度当 tag 会撑爆内存倒排索引）；AlertEngine 三级判定（阈值→毛刺过滤 abnormalSince→冷却去重 lastAlertAt）
- **⑦ MQTT 通信与消息可靠性**（2026-08-26）：Paho 客户端；QoS 语义（0 最多一次 / 1 至少一次可重 / 2 恰好一次不重不丢）；指令用 QoS1 不用 QoS2（省两轮握手，重复投递被 cmdId 幂等兜住）；retained 补收最后一条指令 + `expires` 防过期执行；两套重连（Paho automaticReconnect 管"连上后断线"自动重连并恢复订阅 + 自研 @Scheduled 15s 兜底"首次连接失败"）；cleanSession=true（数据周期上报丢一条可接受，业务层幂等兜底）；Topic 通配符（`#` 任意多层 / `+` 单层，选 `#` 为前缀语义+扩展性）；LWT 抓连接断 + 数据超时 90s 抓"连接没断但数据停了"两层互补
- **⑧ 指令下发与 ACK 闭环**（2026-08-26）：先落库再发 MQTT（① 先持久化可补偿 ② 必须落库才有 cmdId 可进 CSV）；afterCommit 竞态（瞬时指令毫秒级回 ACK，事务未提交时并发 ACK 线程 RR 隔离读不到行→误判未知 ACK 丢失，故事务提交后再发）；ACK 状态机 pending→acked/failed/expired/timeout；定时 pollPendingAcks(30s) 收敛兜底 + 主动 pollAck 即时反馈双路判定；cmdId 协议域整数（不能自增/UUID），MAX+1 按 sensor 分组（WHERE cloud_sensor_id）+ `uk_sensor_cmd` 唯一索引 + 乐观重试替代加锁

### ✅ 数据对标（车辆材料对标）—— 对比查询完整流程

- **对比查询完整流程**（用户选 3 车 + 20 参数）：① 先查 Redis 缓存（key=车型组合+参数集合+版本），命中直接返回（秒开）；② 未命中查库，按所选参数 `WHERE vehicle_id IN(..) AND param_key IN(..)` + 联合索引，避免全量加载；③ **Service 层组装**：`Map<Long, Map<String,String>>` + `computeIfAbsent` 把竖行明细拼成横向对比结构（行转列的业务效果、Java 实现，非 SQL）；④ **回填 Redis 缓存**（设过期时间）——Cache-Aside 读侧闭环；⑤ 返回前端，前端按**参数配置表**动态渲染列（`v-for` config），值从组装结果按 param_key 取。
- **表结构**：三张表——参数配置表（管"有哪些参数"）/ 车辆信息表（管车固定信息）/ 参数值明细表（vehicle_id + param_key + param_value 三列，管"每车每参数的值"）。加参数 = 配置表加一行 → 明细表 param_key 列从此允许该新值（**不是新增列**）；无需改表改码发版。
- **写入**：先校验 param_key 存在于配置表（配置驱动防脏数据）→ 主表插车辆 + 明细表 upsert 各参数值 → 删对应缓存（Cache-Aside 写侧：先写库再删缓存）。

### 🔧 本次代码补充

- `batchWrite` 补逆序回滚（`Collections.reverse`）
- **RabbitMQ 异步解耦完整实现**（12 处：实体/Mapper/配置/生产者/消费者/死信/钉钉/补偿/DDL + pom/yml/docker-compose/controller），`mvn compile` 通过
- 文档三模式动作矩阵对齐代码（max_airflow 默认 2000→4000、风阀 ×5→×3）

### ⚠️ 易错点（拷打中被纠正的）

- 先写表后发 MQ ≠ 防脏数据，而是"先落库保证 MQ 挂了任务也不丢、可补偿"
- Fanout = 广播（忽略 routing key），**不是**前缀匹配；前缀匹配是 Topic
- 自动 ACK（收到就删）是 `none` 模式；默认 `auto` 是"正常返回才 ACK、异常 requeue"
- rollbackFail = 回滚失败（中间态需人工），不是写入失败
- SETNX 幂等两场景：成功留 key（拦重投防重复执行）/ 失败删 key（允许重试）
- **毛刺过滤 vs 冷却去重（InfluxDB 轮）**：毛刺过滤（duration/abnormalSince）管"要不要开始告警"（持续够久才算异常），冷却去重（cooldown/lastAlertAt）管"要不要重复告警"（告警过就歇一会）——别搞反
- **CQ ≠ RP（InfluxDB 轮）**：CQ（降采样 mean）≠ RP（保留时长到期自动删）——两件事，别混
- **tag vs field（InfluxDB 轮）**：tag 有索引、可 WHERE/GROUP BY；field 无索引、只做数值聚合。sensor_id 放 tag，数值测量放 field；别用 MySQL "回表"概念套 InfluxDB
- **tag 基数陷阱（InfluxDB 轮）**：高基数连续值（温度、随机 UUID）绝不放 tag，会撑爆内存倒排索引
- **`GROUP BY time(1h), *` 的 `*`（InfluxDB 轮）**：= 按所有 tag 分组（保各 sensor 独立聚合），**不是**"全部列"；`./.*/` = 正则匹配当前库所有 measurement（新表自动纳入）
- **内存态重启后果（InfluxDB 轮）**：abnormalSince 丢失→毛刺重新计时（持续异常告警延迟）；lastAlertAt 丢失→冷却失效（重复告警）。生产应挪 Redis
- **QoS 语义（MQTT 轮）**：QoS0 不保证不丢（fire-and-forget）/ QoS1 保证不丢但**可重复** / QoS2 不丢**且不重**——别记反"哪个保证不丢、哪个保证不重"
- **指令用 QoS1 不用 QoS2（MQTT 轮）**：QoS2 多两轮握手（PUBREC/PUBREL/PUBCOMP）延迟吞吐吃亏；重复投递被 cmdId 唯一索引幂等兜住，所以 QoS1 够用
- **retained 补收 + expires 防过期（MQTT 轮）**：传感器重连补收最后一条 retained 指令，但可能已过期，必须靠 `expires` 字段判断丢弃
- **两套重连（MQTT 轮）**：Paho automaticReconnect 只管"连上后断线"自动重连并恢复订阅，**不覆盖首次连接失败**；自研 @Scheduled(15s) 兜底首次连不上 / broker 晚于应用启动
- **cleanSession=true（MQTT 轮）**：离线时 broker 丢弃订阅和消息；数据是周期上报丢一条可接受，业务层幂等兜底——用 cleanSession 换内存
- **Topic 通配符（MQTT 轮）**：`#` 任意多层 / `+` 正好一层；`data/#` 是"前缀语义+扩展性"的设计选择，不是必须，协议固定单层用 `+` 更精确
- **LWT vs 数据超时（MQTT 轮）**：LWT 只抓"连接级"异常断线（掉电/断网）；"连接没断但数据停了"（采集卡死）LWT 不触发，只能靠数据超时 90s（>上报周期 60s）兜底
- **afterCommit 竞态（ACK 轮）**：瞬时指令毫秒级回 ACK，事务未提交时并发 ACK 线程按 RR 隔离读不到行→误判"未知指令的 ACK"丢失，所以必须事务提交后再发 MQTT
- **cmdId 是协议域整数（ACK 轮）**：不能自增/UUID（设备要解析、要回带）；MAX+1 按 sensor 分组 = 同一张表 `WHERE cloud_sensor_id=?` 过滤 + `uk_sensor_cmd(sensor_id,cmd_id)` 唯一索引 + 乐观重试替代加锁

### 🔀 简历改动（2026-08-26，空气技研管理平台第一段）

- 简介：~~HON9000 工业规范 / Modbus TCP~~ → **MQTT/Modbus + 下游硬件通信规范**；~~AHU/VAV 空调控制~~ → **空调指令控制**（弱化具体协议厂商名）
- 删除 bullet：~~设备控制指令链路设计（HON9000→Modbus 映射 + 三模式动作矩阵）~~、~~实时推送（Redis Pub/Sub → WebSocket）~~
- 新增 bullet：**MQTT 通信与消息可靠性**（Topic 路由 + QoS + retained + 断线自动重连）
- 新增 bullet：**指令下发与 ACK 闭环**（先写库再发 MQTT + ACK 状态机 + 定时/主动双路判定）
- ⚠️ 注意：MQTT 在 **cams-cgq（传感器模块）**，Modbus 在 **cams-backend（AHU/PLC 控制）**，两个协议分属两个模块；简介"软件到 PLC 硬件控制"表述对 MQTT 侧（控制的是传感器而非 PLC）略模糊，拷打时要说清边界



### 📋 待拷打模块

- ✅ ⑦ MQTT 通信与消息可靠性、⑧ 指令下发与 ACK 闭环 —— **已完成**（见已通关，2026-08-26）
- ⑥ AI 智能助手 + RAG → 已挪至**外卖客服 Agent 项目**（D:\work\agent 外卖客服）单独拷打，不在此处落地

