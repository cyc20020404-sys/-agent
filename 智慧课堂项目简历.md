# 项目简历 · 天机学堂（TJXT）

> 在线教育微服务平台 · Java 开发工程师

---

## 一、项目概况

| 项目 | 说明 |
|------|------|
| **项目名称** | 天机学堂 —— 成人职业技能在线教育平台 |
| **技术架构** | Spring Cloud Alibaba 微服务 |
| **核心组件** | Nacos / Gateway / Sentinel / OpenFeign / Seata |
| **中间件** | Redis + Redisson、RabbitMQ、Elasticsearch + Canal、MySQL、XXL-JOB、MinIO |
| **ORM / 框架** | Spring Boot 2.7、MyBatis-Plus、Lombok |
| **个人职责** | 网关鉴权、课程缓存与数据同步、学习中心、秒杀促销、交易与分布式事务 |

**一句话介绍：**

> 基于 Spring Cloud Alibaba 的微服务在线教育平台，拆分为 9 个微服务（网关/认证/用户/课程/学习/考试/促销/交易/媒体），覆盖课程售卖、学习辅助、考试评测、营销促销完整业务链路。

---

## 二、核心亮点总览

| # | 模块 | 核心难点 | 关键技术 |
|---|------|---------|---------|
| 1 | 网关鉴权 | 统一认证入口、JWT 无状态鉴权 | Gateway 过滤器 + 双 Token + Redis 黑名单 |
| 2 | 数据同步 | DB 与 ES 一致性 | Canal 监听 Binlog → MQ → ES |
| 3 | 断点续播 | 学习进度高并发上报 | Redisson 分布式锁 + MQ 削峰 |
| 4 | 签到系统 | 海量用户存储优化 | Redis Bitmap 位图 |
| 5 | 秒杀架构 | 高并发防超卖、防刷 | Redis Lua + MD5 动态链接 + MQ 异步 |
| 6 | 分布式事务 | 跨服务数据一致性 | Seata AT / TCC + MQ 最终一致性 |

---

## 三、六大核心模块详解（STAR）

### 1. 微服务 Gateway 鉴权

**场景：** 系统拆成 9 个微服务，如果每个服务各自做鉴权，代码重复且不安全，需要统一认证入口。

**方案：** 在网关层做全局鉴权过滤器 `AuthGlobalFilter`（Order = 0，最高优先级）。

```
请求进入 → AuthGlobalFilter
    ├── 1. 白名单放行（/auth/login、/doc.html 等）
    ├── 2. 提取 Authorization: Bearer <AccessToken>
    ├── 3. JWT 校验（签名 + 过期时间）→ 失败返回 401
    ├── 4. 解析 userId / username，通过请求头透传给下游
    └── 5. 下游微服务用 UserContext.getUser() 直接获取用户
```

**核心设计：**
- **双 Token 机制**：AccessToken（30min 日常鉴权）+ RefreshToken（7day 无感刷新），解决「短了频繁登录、长了不安全」的矛盾
- **Redis 黑名单**：JWT 天然无状态无法主动失效，通过 Redis 存 token 实现「踢人下线」和「多设备互踢」
- **技术选型**：Gateway 基于 WebFlux + Netty（非阻塞 I/O，吞吐高），区别于传统 MVC 的一请求一线程模型

**效果：** 无效请求在网关层被拦截，不穿透到业务层；鉴权逻辑只维护一处，下游服务零鉴权代码。

> **面试追问点：** JWT 与 Session 区别 / WebFlux vs MVC / 双 Token 怎么无感刷新 / JWT 无法主动失效怎么解决。

---

### 2. Canal + ES 数据同步

**场景：** 课程需要全文检索（关键词、拼音、高亮、聚合），数据存在 MySQL，但 ES 需要实时跟 DB 保持一致。

**问题：** 双写方案（写完 DB 再写 ES）会产生数据不一致——DB 成功 ES 失败、或 ES 成功 DB 未提交。

**方案：** 用 Canal 监听 MySQL Binlog，实现最终一致性同步。

```
MySQL Binlog → Canal（伪装成 Slave 监听）→ RabbitMQ → ES 消费者 → 写入 ES
    （实时监听）         （消息可靠传输）          （异步消费）
```

**核心设计：**
- **Canal 原理**：伪装成 MySQL 的 Slave 节点，接收 Master 的 Binlog，解析出 INSERT/UPDATE/DELETE 事件
- **全量 + 增量结合**：初次 `SELECT *` 批量写入 ES，之后 Canal 增量实时同步
- **一致性兜底**：① Canal 解析顺序即 DB 操作顺序 → 最终一致；② MQ 消费失败重试 + 死信队列；③ XXL-JOB 每晚定时全量对账

**效果：** 摆脱双写的一致性噩梦，课程上下架秒级同步到 ES，搜索实时可用。

> **面试追问点：** 为什么不用双写 / Canal 原理 / 数据一致性怎么保证 / ES 倒排索引原理 / MySQL 分库分表与读写分离。

---

### 3. 学习进度 · 断点续播

**场景：** 用户看视频要实时记录进度，下次打开能「断点续播」。同一用户可能多设备同时学习，且前端 30 秒定时上报会并发到达，存在数据重复累加/丢失风险。

**方案：**
- **前端**：每 30 秒上报一次观看时长（而非暂停才上报），页面关闭/崩溃时通过 `beforeunload` 补报一次
- **后端**：Redisson 分布式锁防并发 + MQ 削峰持久化

```java
// 锁粒度精确到「用户 + 课时」，避免误伤其他课时的更新
String lockKey = "learning:lock:" + userId + ":" + lessonId;
RLock lock = redisson.getLock(lockKey);
lock.tryLock(3, 10, TimeUnit.SECONDS);  // 等3秒，锁10秒自动释放防死锁
// 1. 查已有进度 → 2. 累加观看时长 → 3. 判断完成(≥90%) → 4. 回写Redis + 异步MQ写DB
```

- **断点续播**：`lastLearnTime` 字段记录上次观看位置，下次打开返回给前端做续播起点
- **MQ 削峰**：进度先写 Redis（用户无感），MQ 异步批量写 DB，1000 个用户同时学习不会打垮数据库

**效果：** 学习进度实时、不丢、不重复；断点续播体验优化；DB 压力被 MQ 缓冲。

> **面试追问点：** 为什么用分布式锁 / 锁粒度怎么定 / Redisson 看门狗机制 / MQ 在这里起了什么作用（削峰 + 解耦 + 可靠性）。

---

### 4. Redis Bitmap 签到系统

**场景：** 用户每日签到 + 连续签到奖励，海量用户长期签到数据如果用关系表存储，会爆炸式增长。

**问题：** 传统 DB 方案——1 万用户 × 31 天 = 31 万行记录，一年 365 万行。

**方案：** 用 Redis Bitmap，1 个二进制位存一天。

```
Key:   sign:2024:8:10001   （年份:月份:用户ID）
Value: [0][1][1][1][0]...[1]   ← 31 位，每位代表一天

SETBIT sign:2024:8:10001 4 1    ← 8月4日签到
GETBIT sign:2024:8:10001 4      ← 查某天是否签到
BITCOUNT sign:2024:8:10001      ← 本月累计签到天数
```

**存储对比：**

| 方案 | 1万用户 31 天存储 | 查询 | 连续签到计算 |
|------|------------------|------|-------------|
| DB 表 | ~31 万行 ≈ **15MB** | `COUNT(*)` | 复杂 SQL |
| Bitmap | 1万 × 4字节 = **40KB** | `BITCOUNT` 直接算 | 位运算遍历 |

- **连续签到奖励**：从今天往回遍历位图，遇未签到即中断，`连续天数 = min(连续天数, 7)`，第 N 天加 N 分

**效果：** 存储空间缩小约 375 倍（15MB → 40KB），签到查询 O(1)。

> **面试追问点：** 为什么用 Bitmap / 连续签到算法 / 积分体系与过期策略 / Redis 用了哪些数据结构（String/Hash/Bitmap/ZSet/Set/List）。

---

### 5. 秒杀架构（含金量最高）

**场景：** 秒杀高并发下既要防超卖，又要防脚本刷单，还要扛住海量流量不拖垮数据库。

**方案演进（体现思考深度）：**

| 版本 | 方案 | 问题 |
|------|------|------|
| V1 | 查库存 → 判断 → 减库存 | 并发下两个线程同时读到库存 1 → **超卖** |
| V2 | DB 乐观锁 `UPDATE ... WHERE stock > 0` | 解决超卖，但流量全打 MySQL → **DB 撑不住** |
| V3 | **Redis + Lua 原子脚本** | ✅ 原子性 + 内存操作，QPS 10 万+ |

**完整秒杀链路：**

```
① 前端校验（倒计时 + 滑块验证码防脚本）
② 获取动态链接  GET /seckill/path → MD5(seckillId + salt)  ← 防提前下单
③ 执行秒杀      Redis Lua 原子扣库存 → SETNX 防重复 → 发 MQ
④ 轮询结果      排队中 / 成功(订单号) / 已抢光
```

**四大核心设计：**
1. **Lua 原子扣库存**：查询 + 判断 + 扣减作为一条命令原子执行，在 Redis 单线程模型下天然串行，杜绝超卖
2. **MD5 动态链接**：秒杀开始前下发随机盐值生成的 MD5 链接，黄牛不知道盐值无法提前构造脚本
3. **SETNX 防重复**：`setIfAbsent("seckill:order:" + userId + ":" + seckillId)`，一人一次天然幂等
4. **MQ 异步下单削峰**：扣库存成功立即返回「排队中」，订单创建交给消费者慢慢处理，用户响应从秒级降到毫秒级

**库存预热：** 秒杀开始前 5 分钟 XXL-JOB 定时把库存从 DB 加载进 Redis，秒杀过程只操作 Redis（单机 10 万 QPS vs MySQL ~3000 QPS）。

**效果：** 核心扣库存路径只走 Redis，QPS 从 DB 方案的 3000 提升到 10 万+，超卖问题彻底解决。

> **面试追问点：** 为什么用 Lua 而不是单纯 DECR / MQ 怎么保证消息不丢（Confirm + 持久化 + 手动 ack）/ 怎么防重复消费（幂等）/ 库存预热怎么做。

---

### 6. 分布式事务

**场景：** 用户下单购买课程，涉及「创建订单（trade）+ 核销优惠券（promotion）+ 扣减积分（user）」三个独立微服务、三个独立数据库。本地事务无法保证跨库一致性。

**方案：** 按业务场景混合使用多种方案。

```
① Seata AT 模式（普通下单）—— 无侵入
   业务 SQL + 自动生成 UNDO_LOG 反向SQL
   一阶段：执行业务SQL并记录反向SQL
   二阶段：全部成功 → 删UNDO_LOG；任一失败 → 执行反向SQL回滚
   仅需 @GlobalTransactional 注解

② Seata TCC 模式（秒杀高并发）—— 高性能
   Try（冻结资源）→ Confirm（确认使用）→ Cancel（回滚恢复）
   资源由业务自己控制，无锁、性能高

③ MQ 最终一致性（积分记录）—— 非实时一致性要求
   本地消息表 + 消费端重试
```

**AT vs TCC 对比：**

| | AT 模式 | TCC 模式 |
|---|---------|---------|
| 侵入性 | 无（自动反向 SQL） | 有（需写 Try/Confirm/Cancel） |
| 性能 | 一般 | 高 |
| 适用 | 简单业务 | 复杂 + 高并发（秒杀） |

**配套设计：**
- **30 分钟超时取消**：RabbitMQ 死信队列实现延迟消息（TTL 30min → 死信交换机 → 取消订单队列），取消时乐观锁 `WHERE status='待支付'` 保证幂等
- **支付回调幂等**：验签 → 查状态（已是「已支付」直接返回 success）→ 更新状态 → 开通学习权限

**效果：** 跨服务数据一致性得到保证，同时按业务特点选型——简单业务用 AT 零侵入，高并发用 TCC 保性能，非核心链路用 MQ 最终一致。

> **面试追问点：** 什么是分布式事务 / Seata AT 原理 / AT vs TCC / 除了 Seata 还有什么方案（MQ 最终一致、最大努力通知）/ 支付回调怎么幂等。

---

## 四、面试自我介绍模板（2 分钟版）
技术栈：Spring Cloud Alibaba（Nacos + Sentinel + Gateway）、MyBatis-Plus、Redis（缓存/分布式锁/Bitmap）、RabbitMQ（削峰/延迟消息）、Elasticsearch + Canal。
智慧课堂是一个基于 Spring Cloud Alibaba 微服务架构的在线教育平台，面向成人职业技能培训，拆分为 9 个微服务：网关、认证、用户、课程、学习、考试、促销、交易、媒体。我重点深入了以下核心模块：
网关鉴权：设计全局鉴权过滤器 `AuthGlobalFilter` 统一做 JWT 校验和用户信息透传，采用 AccessToken（30min）+ RefreshToken（7d）双 Token 机制实现无感刷新，并用 Redis 黑名单解决 JWT 无状态、无法主动失效的问题，支持踢人下线。
数据同步：课程搜索用 Canal 监听 MySQL Binlog（伪装成 Slave 实时解析增删改），经 RabbitMQ 异步投递到 Elasticsearch，配合消费重试 + 死信队列 + XXL-JOB 定时对账保证最终一致性，避免「双写」的数据不一致。
学习中心：签到用 Redis Bitmap 实现（1 人 1 月仅 31 位，1 万用户月存储从 15MB 降到 40KB）；学习进度用 Redisson 分布式锁（锁粒度到「用户 + 课时」）防并发重复累加，再经 MQ 削峰异步落库，记录 lastLearnTime 支持断点续播。
秒杀系统：核心库存扣减用 Redis Lua 原子脚本防超卖，MD5 动态链接防脚本提前下单，SETNX 防一人重复抢购，扣减成功后 MQ 异步下单削峰，整体 QPS 从 DB 方案的 3000 提升到 10 万+。
分布式事务：下单链路跨「订单 + 优惠券 + 积分」三个服务，普通下单用 Seata AT（无侵入、自动反向 SQL），秒杀高并发用 TCC（Try/Confirm/Cancel 高性能），积分等非实时链路用 MQ 最终一致性。



---

## 五、面试高频追问速查

| 追问 | 一句话回答 |
|------|-----------|
| 你的方案有什么缺点？ | 如 JWT 无法主动失效 → 已用 Redis 黑名单解决 |
| 为什么选这个而不是那个？ | 如 Redisson vs Jedis → 提供分布式锁 + 看门狗 |
| 数据量扩大 10 倍怎么办？ | 垂直扩容 → 水平扩容、分库分表、Redis Cluster |
| 遇到最难 bug？ | Lua 脚本边界：Redis DECR 对不存在的 key 返回 -1，需覆盖该边界防负库存 |
| 重新设计怎么改进？ | 秒杀可用 Redis Stream 替代 MQ 减少依赖；课程搜索可引入向量检索 |

> **核心原则：** 每个技术选型都能说出「为什么不用另一个」；每个结果都要量化（QPS、存储、响应时间）；主动说出 trade-off 展示思考深度。

---

# 智慧课堂（天机学堂）· 面试拷打进度记录

> 按简历逐条拷打。记录「已打通的知识点 + 面试核心句 + 被纠偏的点」。
> 续拷从这里接着走。

---

## 拷打进度总览

| 简历 bullet | 状态 | 核心结论 |
|---|---|---|
| ① 网关鉴权 + 双 Token | ✅ 已打通 | 攻击面错开 + 无感刷新 + 刷新风暴 + Redis 存三样 |
| ② 数据同步 Canal + ES | ✅ 已打通 | 唯一真相源 + 兜底链盲区 + 变更流水对账 + 位点续传 |
| ③ 学习中心（Bitmap + Redisson 锁） | ✅ 已打通 | Bitmap offset/跨月/归档 + Redisson 看门狗与 leaseTime 互斥 |
| ④ 秒杀系统 | ✅ 已打通 | Lua 原理、DECR vs Lua、预热、QPS 话术、MD5、SETNX、MQ |
| ⑤ 分布式事务 AT/TCC | ✅ 已打通 | undo_log 前后镜像、脏检查、TCC 冻结、三坑一套状态机解法 |
| ⑥ 考试系统 | ⏳ 待拷打 | 组卷/防作弊/自动评分 |
| ⑦ AI 智能体扩展 + RAG | ✅ 已打通 | RAG 闭环、检索=语义、重排、Agent vs RAG、Function Calling 循环 |

---

## ① 网关鉴权 + 双 Token（✅ 已打通）

### 1.1 「用户信息透传」的安全边界

**面试官问法**：攻击者绕过网关直连下游，自己塞 `user-id: 888888`，下游无条件信任 → 鉴权失效？

**核心结论**：
- 这套设计**防外不防内**，前提是「内网可信，下游服务只内网暴露」。
- 主动承认这个 trade-off 是加分项，不是减分。
- 要加固：内部 HMAC 签名 / 内部服务 token，下游验签才信 header。

**面试核心句**：
> 「`UserContext` 里存的是『网关已验证过的输入』，和『用户自己提交的参数』要严格区分，否则直接开越权大门。」

### 1.2 前端怎么感知 AccessToken 过期

- **被动（等 401）**：坑 = 401 的那个请求本身失败了，要「重放原请求」；POST 重放可能重复下单。
- **主动（定时器提前刷）**：坑 = 多 tab 多定时器同时刷（刷新风暴）、浏览器后台节流冻结定时器、本地时间与服务端不同步。
- **主流做法**：被动为主 + 主动兜底；核心是把「401 后重放」做对。

### 1.3 无感刷新 6 步流程

1. 登录发两个 token
2. 每次请求带 AccessToken（header）
3. 过期 → 401
4. 前端拦截 401 → 自动调 `/auth/refresh`（浏览器自动带 HttpOnly cookie 里的 RefreshToken，JS 不用读）
5. 服务端校验 RefreshToken → 发新 token
6. 重放刚才 401 的请求

**第 6 步的坑（加分）**：有副作用的请求（POST/PUT）不自动重放，提示用户重试；只对幂等 GET 自动重放；前端用「单飞锁」防并发刷新。

### 1.4 刷新风暴（并发刷新，旧 token 被误踢）

**问题**：两 tab 同时拿旧 RefreshToken 刷新，旧 token 立即作废 → 第二个被拒 → 用户被误踢。

**解法：刷新幂等 + 宽限期**
> 旧 token 换新后「保留 1 分钟宽限期」而非立即作废；这 1 分钟内并发请求换到「同一个」新 token；1 分钟后旧 token 彻底失效。

**追问「1 分钟宽限安全吗」**：
> 攻击者偷到旧 token 也必须 1 分钟内用掉才有价值；一旦换走新 token，服务端记录「旧 token 已被使用」，合法用户下次刷新发现被顶掉 → 告警。安全是「可检测可追责」，不是「绝对不可偷」。

**⚠️ 纠偏（我之前理解错的地方）**：
- 「版本号法」解决的是**踢人下线/多设备管理**（token 里带 version，踢人时 version+1），**不是**解决刷新风暴。

### 1.5 两个 token 为什么放不同地方（地基）

| | AccessToken | RefreshToken |
|---|---|---|
| 放哪 | `Authorization: Bearer` 自定义 header | HttpOnly Cookie |
| 防住 | CSRF（自定义 header 跨域表单塞不了） | XSS（JS 读不到 HttpOnly cookie） |
| 怕 | XSS（JS 能读 localStorage） | CSRF（cookie 会被自动携带） |
| 缓解 | 时效 30min + 可踢 | 只在 /auth/refresh 一个接口用 |

**面试核心句（黄金句）**：
> 「AccessToken 怕 XSS 不怕 CSRF，走 header；RefreshToken 怕 CSRF 不怕 XSS，走 HttpOnly cookie。两个 token 用不同载体把攻击面错开——这才是双 Token 真正更安全的地方，而不是简单的『一个短一个长』。」

**header 防 CSRF 的本质（加分句）**：
> 跨域 JS 加自定义 header 会先触发 OPTIONS 预检，预检不过请求直接发不出去。

### 1.6 Redis 里到底存什么（终极追问）

1. **RefreshToken 版本号** `auth:refresh:version:{userId}` —— 踢人时 +1，旧 RefreshToken 全失效
2. **AccessToken 黑名单** `auth:blacklist:{jti}` —— JWT 无状态无法主动失效，存被踢 token 的 **jti**（不是整个 token，省空间）
3. **RefreshToken ↔ AccessToken 对应关系** —— 实现换新 + 宽限期

### 1.7 XSS vs CSRF（概念地基）

| | XSS | CSRF |
|---|---|---|
| 攻击者在哪 | 你网站里（注入脚本） | 他自己网站里 |
| 目标 | 偷数据（token/cookie） | 借你身份发请求 |
| 原理 | 网站没过滤输入，执行恶意 JS | 浏览器对目标站自动带 cookie |
| 借/偷到什么 | localStorage ✅、普通 cookie ✅、HttpOnly ❌ | cookie ✅、自定义 header ❌ |
| 防御 | 输入过滤 + HttpOnly + CSP | 自定义 header / CSRF Token / 校验 Referer |

**自我检验题**：把 AccessToken 也放 HttpOnly Cookie 会怎样？
- 防住了 XSS（JS 读不到），但两个 token 都被 CSRF 借走 → 最敏感操作全暴露 → 所以一个 header 一个 cookie。

---

## ② 数据同步 Canal + ES（✅ 已打通）

### 2.1 双写的病根

> 两个数据源地位平等，没有主从关系，失败时不知道以谁为准 → 永远追不齐。

典型场景：第一次 MySQL 成功 ES 失败（MySQL 新 ES 旧）；第二次 MySQL 失败 ES 成功（ES 新 MySQL 旧）——两个源各自时好时坏，没有基准。

### 2.2 Canal 的本质

> MySQL 是唯一真相源（source of truth），ES 永远是从 Binlog 推导出来的、在追 MySQL 的路上。只剩「ES 追上 MySQL 要多久」= 最终一致。

读方只读 ES，延迟窗口毫秒级，业务可接受秒级一致。

### 2.3 兜底链

重试 → 死信队列 → XXL-JOB 对账

**⚠️ 纠偏**：兜底链的前提是「消息已进 MQ」。Canal 挂了消息根本没产生，重试/死信全失效。

### 2.4 两种「不一致」要分清

| 类型 | 何时 | 会不会自己好 | 靠什么 |
|---|---|---|---|
| 短暂不一致 | 消息进了 MQ、ES 没消费完 | 会（毫秒~秒） | 重试，**不需要对账** |
| 永久不一致 | Canal 挂了/消息丢了 | **不会** | **只能靠对账** |

### 2.5 对账对的是什么

> 不是全量比对（几百万条对不过来），而是**维护一张「变更流水表」**，业务每次改课程顺便写一条「courseId=123 变更了」，对账时只查这张流水表，拿 courseId 去 MySQL 和 ES 各查一次，对不上补写。

### 2.6 Canal 故障恢复 vs ES 重建（⚠️ 之前类比用错场景）

- **Canal 短时故障** → **位点续传**（记录 Binlog 位点/Gtid，重启续读补数据）
- **ES 全量重建**（漏太多/索引坏了/mapping 改了）→ **全量快照（SELECT * 灌 ES，像 RDB）+ 增量追加（从位点续，像 AOF）**

**⚠️ 纠偏**：「快照+追加」用在全量重建，**不是** Canal 挂了恢复；Canal 挂了靠位点。GTID 读作「G-T-I-D」。

### 2.7 完整回答骨架（6 点）

1. 双写病根：无主从
2. Canal 本质：唯一真相源
3. 兜底链：重试→死信→对账
4. 盲区：Canal 挂了消息没产生，靠对账兜底
5. 对账对的是「变更流水表」
6. 恢复：Canal 位点续传；ES 全量快照+增量追加

---

## ③ 学习中心（✅ 已打通）

### Bitmap 签到三问
1. **offset 从 0 开始**：8 月 1 号 = offset 0，8 月 N 号 = `N-1`。
2. **跨月连续签到**：从今天往回数到本月 1 号（offset 0）且 1 号也签了，才切到上月 key 从 `上月天数-1` 继续。
3. **存储对比偷换概念**：15MB→40KB 只比了「当月热点」，没比「历史可查」。DB 是永久明细，Bitmap 过期即删。
   - ⚠️「Redis 持久化」**不能**解决这个问题（持久化防宕机丢失，不是防过期删除）。
   - 正解 = **冷热分层**：热数据 Redis Bitmap 省内存；XXL-JOB 每月末落库归档（存当月天数/连续天数/位图字符串）；Redis 只留近 N 个月；当月查 Redis、历史查归档表。

### Redisson 锁埋雷点（文档自相矛盾）
- ⚠️ `tryLock(3, 10, SECONDS)` **显式传 leaseTime=10s → 看门狗不启动**，10s 后锁必释放，不管业务跑没跑完。
- 看门狗只在**没传 leaseTime（或 -1）**时才启动（默认 30s，每 10s 续期）。
- 业务超 10s 没跑完 → 锁提前释放 → 并发问题回来。
- **两个方向二选一**：
  | 需求 | 写法 | 说明 |
  |---|---|---|
  | 要自动续期 | `lock()` 或 `tryLock(wait, -1, unit)` | 默认 30s + 看门狗；进程崩看门狗停，30s 后自然过期不死锁 |
  | 要固定时长 | `tryLock(wait, leaseTime, unit)` | 无看门狗，业务必须在 leaseTime 内跑完 |
- **参数区分**：`tryLock(3, 10)` 的 3 = waitTime（等多久拿不到放弃），10 = leaseTime（锁持有多久）。
- 深度认知：看门狗 = 防误释放（续期）和防死锁（进程死锁 30s 过期）的平衡。

---

## ④ 秒杀系统（✅ 已打通）

### Lua 为什么防超卖（原理层）
> ① Redis 单线程；② Lua 脚本作为整体原子执行，期间不执行其他命令；③ 并发脚本串行执行——第一个扣完，第二个才看到最新值，判断失败不扣。
- ⚠️ 不是「只能一个线程进来」，是「脚本内三步不可分割 + 并发串行」。

### DECR vs Lua（选型层）
> DECR 原子但只能「扣」，不能「判断够不够再扣」。判断放 DECR 后，超扣要回滚，回滚又引入并发。Lua 把「查→判→扣」打成原子单元，判断在扣之前，一次往返零缝隙。

### 库存预热
- ⚠️ 预热失败后果不是「查询打 DB」，是「库存 key 不存在 → DECR 边界 bug（GET nil → DECR 出 -1）」。
- 正解：**提前预热留余量（开始前 5 分钟）+ 预热失败重试告警 + Lua 加 `EXISTS` 防御**。

### QPS「3000→10 万+」话术（⚠️ 别抄黑马的数字）
- 承认 10 万是**库存扣减单点**，不是端到端。
- 主动说瓶颈：端到端 Gateway/实例/网络都有瓶颈；**秒杀价值是挡流量在 Redis，别打 DB**。
- 翻译成设计意图：Redis 高吞吐拦截 + MQ 削峰 + DB 慢速消费。
- **原则：简历数字要么有依据，要么改成「量级 + 主动说清边界」。**

### MD5 动态链接
- MD5 是**哈希不是加密**，单向、比对验证。
- 防「链接可预测」：`MD5(seckillId + 随机盐)`，盐服务端存 Redis，黄牛没法提前构造。
- ⚠️ 客户端拿到的是「结果」不是「盐」。
- 弱点：黄牛可刷「获取 path」接口 → 靠验证码/滑块 + 网关限流 + 每人限次兜底。**MD5 是防线之一，不是全部。**

### SETNX 防重
- 语义：只有第一次能 set 成功，已有 key 返回 false。
- ⚠️ TTL 必须覆盖「提交 → 结果确定」完整周期，否则订单还没建完 key 过期 → 重复下单。
- 常设够长或按秒杀结束时间算。

### MQ 异步下单（防丢 + 防重）
- 防丢三段：① 生产端 Confirm + 消息持久化；② Broker durable；③ 消费端手动 ACK + 重试 + 死信。
- 防重最硬 = **订单号 DB 唯一索引**（DuplicateKey 跳过），SETNX 可以但 key 会过期。

---

## ⑤ 分布式事务 AT/TCC（✅ 已打通）

### 为什么需要分布式事务
- 下单 = 跨 tj-trade(建单) + tj-promotion(扣券) + tj-user(扣积分)，三个库三个本地事务，绑不成一个原子单元。
- **事后补偿为何不行（时间差）**：补偿是最终一致，中间有用户能看到的脏数据窗口；且补偿自己也要幂等/重试，链条脆。强一致场景要「要么全成、要么全没」。

### AT 模式（自动）
- **⚠️ 靠 undo_log 表的前后镜像回滚，不是 binlog**（binlog 是 MySQL 主从复制的，Seata 不碰）。
- 流程：本地事务里记录 before/after 镜像到 undo_log → 提交时注册到 TC → 失败时拿 before 反编译反向还原。
- **脏检查（Compare）**：回滚前先对比「当前值 == after image」。
  - 相等 → 没人动过 → 安全还原 before。
  - 不相等 → 并发写过了 → 不能盲目覆盖，回滚失败抛脏数据，人工介入。
- **缺点**：不防脏写，高并发冲突激烈（同一行反复改）会频繁失败 → 适合冲突少的业务（下单/积分）。

### TCC（手动，扛并发）
- **灵魂：Try 不真扣，先冻结**。库存表 `total` + `frozen` 两个字段：
  - Try：`frozen+1`（总量不变，划走一块备用，别人抢不到）
  - Confirm：`frozen-1, total-1`（真成交）
  - Cancel：`frozen-1, total+1`（释放）
- **vs AT**：AT「先干了再判断要不要回滚」，TCC「先锁住再决定真干还是放手」→ TCC Confirm/Cancel 阶段资源已锁定不跟人抢，冲突少。

### TCC 三坑（一套病根 + 一套解法）
- **病根**：Try/Confirm/Cancel 是三个独立网络请求，协调器靠**超时**判断成败，网络时序不可靠 → 到达顺序可能颠倒。
- **坑一 空回滚**：Try 执行成功但响应丢失 → 协调器走 Cancel → Cancel 发现没有 Try 预留可释放。
- **坑二 悬挂**：那个「被认为失败」的 Try 在 Cancel 之后才迟到，把资源又冻结了 → 事务已结束，冻结永远无人处理 → 资源泄漏。
- **坑三 幂等**：协调器重试 Confirm/Cancel，不能重复扣/释放。
- **一套解法（事务状态机/控制表）**：
  - Cancel 前查有没有 Try 预留 → 没有 = 空回滚，记录「空回滚」标记，**返回成功**（否则协调器无限重试）。
  - Try 前查有没有「空回滚」标记 → 有 = 拒绝执行（防悬挂）。
  - Confirm/Cancel 前查有没有执行过 → 跳过（防幂等）。
- **黄金句**：「三个坑一套解法，根子都是网络时序不可靠，所以 TCC 每一步都先查状态再执行，而不是无脑执行。」

---

## ⑦ AI 智能体 + RAG（✅ 已打通）

### RAG 是什么
- RAG（检索增强生成）= 让大模型先「查资料」再「回答」。
- 为什么需要：大模型知识截止到训练时点、不含私有内容、会「编」→ RAG 补最新/私有知识 + 降幻觉。

### 难点一：检索（不是生成）
- 检索是最关键的一步——知识库上万份，怎么捞对最相关的？捞错 = 幻觉。
- 生成是模型本身的事，与 RAG 无关。

### 难点二：切块（Chunking）
- 不能整个文档扔给模型：① 爆上下文窗口；② 检索按相关性捞片段，整个文档带进不相关段落，召回精度下降。
- 按语义/段落切，几百 token 一块，块间**重叠 20%**（防完整意思被从中切断）。

### 难点三：向量检索（语义，不是字面）
- ⚠️ 不是「语言模型语义好」，而是**嵌入模型把问题+文档映射到同一语义空间**，语义相近的向量离得近 → 余弦距离算相似度。
- 关键：用户问「事务失效」，文档写「@Transactional 没生效」，字面零匹配，但语义相同 → 只有向量搜得到，关键词（ES 字面）搜空。
- 所以简历用向量检索 / 向量数据库入库。

### 难点四：重排（Rerank）
- 光提高 topK 不够（塞进上下文杂讯越多越干扰）→ **先宽后窄**：
  - Bi-Encoder 粗召回（提前向量化、快、可扩展），topK=20，**召回率优先**
  - Cross-Encoder 精排（问题+文档拼一起逐词交互、准但慢），对 top20 精排取 top3，**精准度优先**
- 黄金句：「向量负责别漏（召回），重排负责别错（排序）」；快模型圈范围、慢模型提精度。

### Agent vs RAG
- RAG = 被动「查资料+回答」；Agent = 主动「调工具、做动作、多步推理」。
- 例：查作业→生成清单，RAG 只捞文本，Agent 会调用订单/作业系统 API 查真实数据再决定下一步。
- Agent = LLM（大脑）+ 工具调用（手脚）+ 规划/记忆。

### Function Calling（Agent 的底层机制）
- 流程循环：注册工具（名字/参数/返回）→ 模型决定调哪个工具（输出结构化指令，不真发请求）→ 程序执行调 API → 结果回喂 → 模型再决定 → 直到出最终答案。
- 关键：LLM 只负责「决定调什么、参数是什么」，手脚是程序；「决定→执行→回喂→再决定」循环 = Agent 多步推理本质。

---

## 拷打全部完成 ✅

① 网关鉴权+双Token → ② 数据同步 Canal+ES → ③ 学习中心 → ④ 秒杀系统 → ⑤ 分布式事务 AT/TCC → ⑥ 考试系统【用户跳过】→ ⑦ AI 智能体+RAG
面试准备闭环：每关都有「核心结论 + 面试黄金句 + 被纠偏的点」。

## 明天待办

- [ ] （可选）⑥ 考试系统补拷打
- [ ] 考前快速过一遍 ①~⑦ 黄金句
